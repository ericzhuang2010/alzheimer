# Phase 18 sex/APOE bidirectional complex KDA aggregation

## Status

**Deprecated for current candidate selection.**

This directory preserves the historical strict-reference Phase 18 analysis for
reproducibility. It must not be used as the current KDA candidate authority.
No active replacement Phase 18 bundle is defined.

Current KDA reporting and candidate selection use:

```text
results/minerva_production/20_sex_apoe_kda_combo/
```

That Phase 20 bundle aggregates the frozen run-level evidence in:

```text
results/minerva_production/12_rosmap_sex_apoe_mito_kda_runs/
```

## Historical contents

- `call_key_driver_returns.tsv`: complete explicit gene-by-run test universe.
- `call_key_driver_significant_returns.tsv`: significant returned rows retained
  for provenance.
- `archive (deprecated)/`: superseded historical Phase 18 outputs.

The scientific tables are preserved unchanged. The descriptive directory name
and `(deprecated)` suffix communicate scope and status only.
