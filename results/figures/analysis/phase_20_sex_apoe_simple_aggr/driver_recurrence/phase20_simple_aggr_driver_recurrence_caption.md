# Phase 20 simple-aggregation driver recurrence

The 20 most recurrent non-MT key drivers across sex/APOE-by-broad-cell categories. Bar length is the number of categories containing the gene. Fill records how many of those category occurrences combine two or more significant call returns by ACAT; the remainder are one-call q passthroughs.
