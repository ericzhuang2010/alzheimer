# SEA-AD network-specific simple-aggregation top-five candidates

Up to five SEA-AD non-MT key drivers per sex/APOE-by-broad-cell category using `SEAAD_A9_2024_RIMBANET_v1`, split into female and male panels. Blue hatched tiles represent scores ACAT-combined across at least two significant call returns; solid orange tiles represent a single within-call q passthrough. The redundant pattern encoding preserves the distinction in grayscale. Categories without a non-MT return are omitted; 5 categories and 20 gene-category rows remain.
