# Figure captions

## Figure 3

**Figure 3. Sex-shared and sex-divergent mitochondrial transcriptional responses in AD.** Mitochondrial-restricted Yu analogue using the Phase 10 `core_mito` universe. Panel A displays stored highest- and lowest-similarity Zhang–Yu rank sets over nominal paired dimensions 162. Heatmap cells are observed paired-state occurrence counts, grouped as Same (green), Different (orange), and Opposite (purple). The `(0,0)` count remains in each score denominator and companion validation but is not a displayed tile; missing states are excluded rather than assigned to `(0,0)`. Row labels give the stored score and observed/nominal coverage; `*` marks stored directional BH FDR <= 0.05. Panel B uses the stored top and bottom 200 score-tail queries and Phase 11 one-sided ORA for Human MSigDB C2:CP v2026.1.Hs. GeneRatio = k/n, BackgroundRatio = M/N, and fold enrichment = (k/n)/(M/N); point x, size, and color show GeneRatio, overlap k, and within-query BH FDR. Filled points pass BH FDR < 0.05 and open points do not. The first 15 tested pathways with at least one matched query gene are displayed in stored statistical order regardless of FDR, so a highest-similarity tail remains visible even when none of its pathways passes FDR. Queries: Highest similarity (n=200, N=700; 0 FDR-significant; 241 pathway matches; 15 displayed); Lowest similarity (n=200, N=700; 81 FDR-significant; 292 pathway matches; 15 displayed). `all_mito_related` and MitoPathways are sensitivity profiles and are not used in this primary figure.

## Figure 4

**Figure 4. APOE e2-shared and e2-divergent mitochondrial transcriptional responses in AD.** Mitochondrial-restricted Yu analogue using the Phase 10 `core_mito` universe. Panel A displays stored highest- and lowest-similarity Zhang–Yu rank sets over nominal paired dimensions 108. Heatmap cells are observed paired-state occurrence counts, grouped as Same (green), Different (orange), and Opposite (purple). The `(0,0)` count remains in each score denominator and companion validation but is not a displayed tile; missing states are excluded rather than assigned to `(0,0)`. Row labels give the stored score and observed/nominal coverage; `*` marks stored directional BH FDR <= 0.05. Panel B uses the stored top and bottom 200 score-tail queries and Phase 11 one-sided ORA for Human MSigDB C2:CP v2026.1.Hs. GeneRatio = k/n, BackgroundRatio = M/N, and fold enrichment = (k/n)/(M/N); point x, size, and color show GeneRatio, overlap k, and within-query BH FDR. Filled points pass BH FDR < 0.05 and open points do not. The first 15 tested pathways with at least one matched query gene are displayed in stored statistical order regardless of FDR, so a highest-similarity tail remains visible even when none of its pathways passes FDR. Queries: Highest similarity (n=200, N=708; 0 FDR-significant; 283 pathway matches; 15 displayed); Lowest similarity (n=200, N=708; 58 FDR-significant; 294 pathway matches; 15 displayed). `all_mito_related` and MitoPathways are sensitivity profiles and are not used in this primary figure.

## Figure 5

**Figure 5. APOE e4-shared and e4-divergent mitochondrial transcriptional responses in AD.** Mitochondrial-restricted Yu analogue using the Phase 10 `core_mito` universe. Panel A displays stored highest- and lowest-similarity Zhang–Yu rank sets over nominal paired dimensions 108. Heatmap cells are observed paired-state occurrence counts, grouped as Same (green), Different (orange), and Opposite (purple). The `(0,0)` count remains in each score denominator and companion validation but is not a displayed tile; missing states are excluded rather than assigned to `(0,0)`. Row labels give the stored score and observed/nominal coverage; `*` marks stored directional BH FDR <= 0.05. Panel B uses the stored top and bottom 200 score-tail queries and Phase 11 one-sided ORA for Human MSigDB C2:CP v2026.1.Hs. GeneRatio = k/n, BackgroundRatio = M/N, and fold enrichment = (k/n)/(M/N); point x, size, and color show GeneRatio, overlap k, and within-query BH FDR. Filled points pass BH FDR < 0.05 and open points do not. The first 15 tested pathways with at least one matched query gene are displayed in stored statistical order regardless of FDR, so a highest-similarity tail remains visible even when none of its pathways passes FDR. Queries: Highest similarity (n=200, N=686; 0 FDR-significant; 231 pathway matches; 15 displayed); Lowest similarity (n=200, N=686; 53 FDR-significant; 295 pathway matches; 15 displayed). `all_mito_related` and MitoPathways are sensitivity profiles and are not used in this primary figure.

## Figure 6

**Figure 6. APOE-dependent sex divergence in mitochondrial transcriptional responses in AD.** Mitochondrial-restricted Yu analogue using the Phase 10 `core_mito` universe. Panel A displays stored highest- and lowest-similarity Zhang–Yu rank sets over nominal paired dimensions 54. Heatmap cells are observed paired-state occurrence counts, grouped as Same (green), Different (orange), and Opposite (purple). The `(0,0)` count remains in each score denominator and companion validation but is not a displayed tile; missing states are excluded rather than assigned to `(0,0)`. Row labels give the stored score and observed/nominal coverage; `*` marks stored directional BH FDR <= 0.05. Panel B uses the stored top and bottom 200 score-tail queries and Phase 11 one-sided ORA for Human MSigDB C2:CP v2026.1.Hs. GeneRatio = k/n, BackgroundRatio = M/N, and fold enrichment = (k/n)/(M/N); point x, size, and color show GeneRatio, overlap k, and within-query BH FDR. Filled points pass BH FDR < 0.05 and open points do not. The first 15 tested pathways with at least one matched query gene are displayed in stored statistical order regardless of FDR, so a highest-similarity tail remains visible even when none of its pathways passes FDR. Queries: APOE e2 - highest similarity (n=200, N=732; 0 FDR-significant; 276 pathway matches; 15 displayed); APOE e2 - lowest similarity (n=200, N=732; 39 FDR-significant; 301 pathway matches; 15 displayed); APOE e33 - highest similarity (n=200, N=705; 0 FDR-significant; 267 pathway matches; 15 displayed); APOE e33 - lowest similarity (n=200, N=705; 52 FDR-significant; 310 pathway matches; 15 displayed); APOE e4 - highest similarity (n=200, N=679; 0 FDR-significant; 286 pathway matches; 15 displayed); APOE e4 - lowest similarity (n=200, N=679; 29 FDR-significant; 273 pathway matches; 15 displayed). `all_mito_related` and MitoPathways are sensitivity profiles and are not used in this primary figure.

