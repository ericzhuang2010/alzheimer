RESOLVED: the mismatch was caused by top-coding exact ages above 90.

The previous pipeline converted all 99 eligible `90+` donors to age 90.
Yu's checksum-verified `dataset_707_basic_02-08-2022.clean.txt` retains exact
ages from 90.04 to 108.28. Using those donor-specific ages reproduced all 716
Vasculature DEG rows exactly across the 29 estimable comparisons:

- recall: 100%
- precision: 100%
- median absolute raw-p difference: 8.47e-18
- median absolute adjusted-p difference: 1.21e-15

PMI, diagnosis, sex, and APOE matched. Software versions, covariate scaling,
MAST settings, and BH scope were not responsible.

## Corrected production approach

1. Phase 02 reads
   `data/processed/dataset_707_basic_02-08-2022.clean.txt`, verifies SHA-256
   `76a71814b43c9fa3e84b9bbb119dddc3fd4b08743948f75ca38400e9bcb7425e`,
   and requires exact uncensored numeric age.
2. Phase 05 attaches the exact donor age and its scaled representation to
   every nucleus while preserving counts and LogNormalize expression.
3. Phase 08 uses `nCount_RNA`, `age_death_scaled`, and `pmi_scaled` in
   Seurat MAST. Scaling preserves the same model space as Yu's raw numeric
   age and PMI.
4. BH is applied within each cell-type × contrast over genes returned after
   `min.pct = 0.1`; DEG calls require FDR < 0.05 and
   `abs(logFC) > log2(1.3)`.

The corrected local Phase 02, Phase 05, and Phase 08 runs are
`validated_complete`, and the Yu comparison alignment tier is `exact`.